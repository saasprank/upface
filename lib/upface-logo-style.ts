import type { CSSProperties } from 'react'

/** Masque le fond noir du PNG logo sur fond sombre (#080C14 / headers). */
export const UPFACE_LOGO_IMG_STYLE: CSSProperties = {
  display: 'block',
  mixBlendMode: 'screen',
  borderRadius: '50%',
}
